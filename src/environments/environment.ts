// Configuración de entornos para la aplicación
export const environment = {
  production: false,
  apiUrl: '', // En desarrollo, usar proxy (URL vacía = relativa)
  backendHost: 'http://localhost:8080' // Solo para debugging
};
